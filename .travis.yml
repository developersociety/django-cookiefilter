dist: xenial

language: python

cache: pip

python:
  - 3.4
  - 3.5
  - 3.6
  - 3.7

env:
  - DJANGO="1.8"
  - DJANGO="1.11"
  - DJANGO="2.0"
  - DJANGO="2.1"

install:
  - pip install codecov tox-travis

script:
  - tox

after_success:
  - codecov

stages:
  - lint
  - test

matrix:
  exclude:
    - python: 3.6
      env: DJANGO="1.8"
    - python: 3.7
      env: DJANGO="1.8"
    - python: 3.4
      env: DJANGO="2.1"

jobs:
  include:
    - stage: lint
      python:
        - 3.7
      env: []
      script:
        - tox -e lint
